# Echo Calculator

This built-in example demonstrates an isolated floating plugin view, Echo theme tokens, keyboard input, and namespaced plugin storage. It is installed only after review and is disabled by default.
